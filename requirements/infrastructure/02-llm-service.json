{
  "requirementText": "Use Langchain to implement the LLM Service. The implementation should be hard coded to Anthropic Claude for the moment. The model ID is 'claude-3-7-sonnet-latest'. Expect the Anthropic API key in the environment variable `ANTHROPIC_API_KEY`. Set the max tokens to 25000. Stream the output of the LLM to stdout. Set the temperature to zero."
}
