{
  "name": "generateInfrastructure",
  "description": "As a User I can generate the infrastructure code from infrastructure requirements and the existing domain model. The files are written into the target directory (default: 'generated'). The command returns the number of generated files."
}
