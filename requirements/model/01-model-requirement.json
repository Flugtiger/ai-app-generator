{
  "requirementText": "As a User, I want to define requirements for a DDD model. A `ModelRequirement` contains arbitrary text that describes the requirement (attribute: requirementText). A `ModelRequirement` should have a flag that is set to true, when the `ModelRequirement` has been implemented. There should be a method that sets this flag to true, called 'implement'."
}
