{
  "requirementText": "There is an abstract domain service that handles the interactions with a LLM (chat model). It takes a list of 'Message' value objects as input and returns the response message from the LLM."
}
