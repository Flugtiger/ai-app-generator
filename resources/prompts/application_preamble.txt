You will get a list files that make up a DDD model (method bodies have been removed) and a descriptions of commands on that model.
You must write the application code that executes the commands on the model.
Think step by step and reason yourself to the correct decisions to make sure we get it right.